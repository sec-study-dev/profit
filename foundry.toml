[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "strategies"
solc = "0.8.26"
optimizer = true
optimizer_runs = 200
via_ir = true
fs_permissions = [{ access = "read", path = "./" }]

[rpc_endpoints]
mainnet = "${RPC_URL}"
bsc = "${BSC_RPC_URL}"

[fuzz]
runs = 64
